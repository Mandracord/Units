# Units

## Notice on Packets

This  addon sends a single client request packet (0x115) to refresh your Limbus Unit totals.

This is the same packet the game sends when you open the currency menu — nothing is being modified or altered. 

## Commands

`//units show | hide` - Display | Hide the HUD
